# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local state/config
.claude/
CLAUDE.md
.mcp.json

# Tauri updater signing key - generated via `tauri signer generate`, should
# live in ~/.tauri/ (outside the repo) per the setup instructions, but this
# is a defensive backstop in case one ever ends up in-tree.
*.key
.tauri/
